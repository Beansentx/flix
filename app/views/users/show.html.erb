<section class="user">

    <%= profile_image(@user) %>
    <h1><%= @user.name %></h1>
    <h2><%= mail_to(@user.email) %> member since <%= @user.created_at.strftime("%B %Y") %></h2>
 
</section>